//   ///
//  Import LIBRAIRES
import 'package:flutter/material.dart';
//  Import FILES

//  //   ///

class Profile extends StatelessWidget {
  const Profile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Peofile Page'),
      ),
      body: const Text('Profile Page'),
    );
  }
}
